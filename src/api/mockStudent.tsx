import { TypeStudent } from "../types/typesComponent"

export const Student = () => {
    const mock:TypeStudent = {
        "name": "federico",
        "surname": "micca",
        "login": "string",
        "session": true
    }
    return mock
}